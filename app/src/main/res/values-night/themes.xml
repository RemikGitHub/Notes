<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Note" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryVariant">@color/accent</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/accent</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/background_dark</item>
        <!-- Font. -->
        <item name="android:fontFamily">@font/ubuntu_medium</item>
        <item name="fontFamily">@font/ubuntu_medium</item>
        <item name="colorSearchBackground">@color/search_background_dark</item>
        <item name="colorSearchIcon">@color/search_icon_dark</item>
        <item name="colorNoteDate">@color/note_date_color_dark</item>
        <item name="colorOptionsBackground">@color/note_options_color_dark</item>
        <item name="colorCardClick">@color/card_click</item>
    </style>

    <style name="ToolBarStyle" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryVariant">@color/accent</item>
        <item name="android:fontFamily">@font/ubuntu_bold</item>
    </style>
</resources>